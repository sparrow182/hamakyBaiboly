{% extends "base.html.twig" %}

{% block body %}

    {{ form(formSearch) }}

    <section>
        {% if searchResults is defined and searchResults | length > 0 %}
            <h2>{{ 'search.result' | trans }}</h2>
            {% for content in searchResults %}
                <p><strong>{{ content.verse }}.</strong> {{ content.text }}</p>
            {% endfor %}
        {% endif %}
    </section>

{% endblock %}

{% block javascripts %}

{% endblock %}
